define(['jquery', 'lib/components/base/modal', 'lib/network/leads/api', './lib/intlTelInput/js/intlTelInput.js'], function ($, Modal) {

    var CustomWidget = function () {
        var self = this,
            system = self.system(),
 
        return this;

    };

    return CustomWidget;
});